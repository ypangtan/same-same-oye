<div class="mb-3 row">
    <label for="{{ $label }}" class="col-sm-5 col-form-label">{{ $label }}</label>
    <div class="col-sm-7">
        <textarea name="{{ $id }}" class="form-control" id="{{ $id }}" cols="30" rows="10"></textarea>
        <div class="invalid-feedback"></div>
    </div>
</div>